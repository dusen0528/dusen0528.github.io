---
title: 비명 감지 AI 모델 및 실시간 음성 이벤트 감지 파이프라인 구축
category: AI Service
summary: 비명 감지 및 응급 상황 감지 모델을 설계하고, 실시간 운영 환경에 적용 가능한 제어 파이프라인까지 연결한 사례입니다.
icon: bi-soundwave
featured: true
order: 4
stack:
  - ResNet
  - FFT
  - FastAPI
  - WandB
highlights:
  - 비명 감지 및 응급 상황 감지 음성 DL 모델 설계
  - FFT 특징 추출과 Noise Injection을 적용한 ResNet 모델 개발
  - AI 기능을 기존 영상 관제 시스템에 실제 서비스로 통합
---
## 기술적 문제 정의

기존 영상 관제 솔루션에 처음으로 AI 기능을 도입하는 과정에서, 단순히 모델을 이식하는 수준이 아니라 실시간 환경의 변동성을 제어하는 서비스 파이프라인 구축이 필요했습니다.

오픈 데이터로 학습한 모델은 실제 현장의 소음과 입력 패턴 차이로 인해 성능 저하가 발생할 수 있었고, 이를 운영 가능한 수준으로 만드는 것이 핵심 과제였습니다.

## 기술적 고민과 검증

### 1. 도메인 시프트 대응

실제 환경 음성을 수집하고 입력 특성을 분석해 FFT 기반 특징 추출 로직을 설계했습니다.

Noise Injection을 적용한 ResNet 18/34 기반 비명 감지 모델을 직접 구축했고, WandB로 실험을 관리하며 지연 시간과 오탐률의 균형점을 찾았습니다.

### 2. 실시간 추론 파이프라인 구성

FastAPI 기반 제어 API를 구축해 설정 변경, 상태 확인, 재시작 제어가 가능하도록 했고, 실제 운영 환경에서도 안정적으로 동작할 수 있는 구조를 마련했습니다.

### 3. 서비스 통합 관점의 설계

모델 하나를 만드는 데서 멈추지 않고 기존 영상 관제 시스템 안에서 다른 기능과 함께 운영될 수 있도록 전체 구조를 고려했습니다.

## 성과

비명 감지 모델은 스마트 규제 샌드박스 사업에 적용되어 실제 납품으로 이어졌습니다.

이 경험을 통해 데이터 수집부터 모델 학습, 실시간 추론 파이프라인 설계, 운영 제어 구조 구성까지 AI 기능을 서비스 단위로 완성하는 엔지니어링 역량을 쌓았습니다.
